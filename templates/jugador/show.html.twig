{% extends 'base.html.twig' %}

{% block title %}Jugador{% endblock %}

{% block body %}
    <h1>Jugador</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jugador.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ jugador.Nombre }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ jugador.Foto }}</td>
            </tr>
            <tr>
                <th>Fecha_Nac</th>
                <td>{{ jugador.FechaNac ? jugador.FechaNac|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nacionalidad</th>
                <td>{{ jugador.Nacionalidad }}</td>
            </tr>
            <tr>
                <th>Dorsal</th>
                <td>{{ jugador.Dorsal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jugador_index') }}">back to list</a>

    <a href="{{ path('jugador_edit', {'id': jugador.id}) }}">edit</a>

    {{ include('jugador/_delete_form.html.twig') }}
{% endblock %}
